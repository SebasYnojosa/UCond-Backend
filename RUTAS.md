# RUTAS

## Autenticación

### Registro de usuario
* `POST /api/auth/sign-up`
* Devuelve: `{token: *token de autenticación*}`

### Inicio de sesión
* `POST /api/auth/sign-in`
* Devuelve: `{token: *token de autenticación*}`